import { BuildConfigurationDefaulted, BuildOptionsDefaulted } from './lib';
import { RuntimeDistribution } from './runtimeDistribution';
export declare class ArgumentBuilder {
    private config;
    private options;
    private rtd;
    constructor(config: BuildConfigurationDefaulted, options: BuildOptionsDefaulted, rtd: RuntimeDistribution);
    buildCmakeCommandLine(): Promise<string>;
    buildGeneratorCommandLine(stagingDir: string): string;
    buildDefines(): Promise<[string, string][]>;
}
//# sourceMappingURL=argumentBuilder.d.ts.map